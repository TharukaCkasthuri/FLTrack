from hparams import HParams


model_hparams = {
    "c1": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.001,
        weight_decay=0.01,
    ),
    "c2": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c3": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c4": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c5": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c6": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c7": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.0001,
    ),
    "c8": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c9": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.0001,
    ),
    "c10": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.001,
        weight_decay=0.01,
    ),
    "c11": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c12": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c13": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c14": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.0001,
    ),
    "c15": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.0001,
    ),
    "c16": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.0001,
    ),
    "c17": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.0001,
    ),
    "c18": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.001,
        weight_decay=0.0001,
    ),
    "c19": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c20": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.0001,
    ),
    "c21": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.001,
    ),
    "c22": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.01,
        weight_decay=0.0001,
    ),
    "c23": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.001,
        weight_decay=0.0001,
    ),
    "c24": HParams(
        batch_size=256,
        num_epochs=250,
        learning_rate=0.001,
        weight_decay=0.001,
    ),
}
